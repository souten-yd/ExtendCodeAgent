#!/usr/bin/env bash
set -euo pipefail
repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
export PYTHONPATH="${repo_root}/src:${repo_root}${PYTHONPATH:+:${PYTHONPATH}}"
exec "${repo_root}/.venv/bin/python" "${repo_root}/tools/local/b0b_confirmation_runner.py" "$@"
