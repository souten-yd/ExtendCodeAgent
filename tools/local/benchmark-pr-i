#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
target_root="${1:-${repo_root}}"
cd "${repo_root}"
"${repo_root}/.venv/bin/python" tools/local/benchmark_pr_i.py "${target_root}"
