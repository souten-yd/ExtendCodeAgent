#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
PYTHONPATH="${repo_root}/src" "${repo_root}/.venv/bin/python" \
  "${repo_root}/tools/local/pr_g_evaluate.py" "$@"
