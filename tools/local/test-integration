#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
cd "${repo_root}"
"${repo_root}/.venv/bin/pytest" tests/integration
"${repo_root}/tools/local/test-opencode-adapter"
