#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
PYTHONPATH="${repo_root}/src" exec "${repo_root}/.venv/bin/python" \
  "${repo_root}/tools/local/b0a_bootstrap.py" "$@"
