# ExtendCodeAgent

ExtendCodeAgent is a host-neutral Project Intelligence layer intended to integrate with
OpenCode through thin adapters without forking OpenCode. The current implementation is
the PR-A foundation: contracts, configuration and capability policy, and provider-neutral
model-routing contracts with offline fake adapters.

Local validation:

```bash
tools/local/bootstrap
tools/local/all-fast
tools/local/build
```
